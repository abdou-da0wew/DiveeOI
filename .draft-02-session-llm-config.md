# Draft 02 — Session/LLM/Config (files 5-8)

## session/llm/request.ts (216 lines)
- PrepareInput: user, sessionID, parentSessionID, model, agent, permission, system[], messages ModelMessage[], small flag, tools Record<string,Tool>, provider, auth, plugin, flags, isWorkflow
- Prepared: system[], messages, tools, params {temperature,topP,topK,maxOutputTokens,options}, messageTransformOptions, headers
- USER_AGENT = opencode/version
- prepare = Effect.fn: builds system array joining agent.prompt + provider system prompt + input system + user.system; triggers plugin experimental.chat.system.transform; handles header dedup; picks variant based on small && model.variants[user.variant]; base options via ProviderTransform.smallOptions or ProviderTransform.options(model,sessionID,providerOptions); mergeDeep chain base->model.options->agent.options->variant; Azure completionUrls cleanup; OpenAI OAuth instructions handling; messages = isOpenaiOauth||isWorkflow ? input.messages else system mapped to system role + input.messages; plugin trigger chat.params for temperature/topP/topK/maxOutputTokens/options; plugin trigger chat.headers; resolveTools filtered; Copilot noop tool injection; opencodeProjectID from InstanceState if opencode provider; returns system/messages/tools sorted/params/headers with opencode headers or session-affinity headers + model headers
- resolveTools: Permission.disabled + filter by user.tools false
- hasToolCalls: iterate messages content for tool-call/tool-result

## config/config.ts (706 lines)
- Imports LayerNode, FSUtil, serviceUse, path, mergeDeep, Global, Flag, Auth, Env etc
- mergeConfig: mergeDeep; mergeConfigConcatArrays dedup instructions; normalizeLoadedConfig strips legacy theme/keybinds/tui
- substituteWellKnownRemoteConfig: ConfigVariable.substitute for url and headers
- resolveLoadedPlugins: ConfigPlugin.resolvePluginSpec per spec
- Info = ConfigV1.Info + plugin_origins, State = {config, directories, deps Fibers, consoleState}
- Interface: get, getGlobal, getConsoleState, update, updateGlobal, invalidate, directories, waitForDependencies, permission
- Service = @opencode/Config
- Helpers: globalConfigFile picks first existing of opencode.jsonc/json/config.json; patchJsonc recursive jsonc-parser modify/applyEdits; writable strips plugin_origins; writableGlobal removes empty shell
- layer Effect.gen: deps FSUtil, Auth, Account, Env, Npm, HttpClient; readConfigFile; fetchRemoteJson with filterStatusOk, HTML re-auth detection via content-type/html check, decode via Schema.fromJsonString; loadConfig via ConfigVariable.substitute + ConfigParse.jsonc + schemaLenient + resolveLoadedPlugins + $schema injection; loadFile; diveeAgentPaths .diveeagent/diveeoi.jsonc; loadGlobal merges config.json, opencode.json, opencode.jsonc, diveeAgent paths, legacy toml migration; cachedGlobal via cachedInvalidateWithTTL infinity; getGlobal; ensureGitignore writes default gitignore if missing; loadInstanceState: large Effect — authEnv, consoleManagedProviders, activeOrgName, pluginScopeForSource (url->global, env content->local, containsPath), mergePluginOrigins dedup via ConfigPlugin.deduplicatePluginOrigins, merge helper, loop auth wellknown fetch (/.well-known/opencode -> remote_config -> fetch remote json -> merge), loadGlobal, Flag.OPENCODE_CONFIG custom file, Flag.OPENCODE_DISABLE_PROJECT_CONFIG check, load opencode files via ConfigPaths.files, directories via ConfigPaths.directories, for each dir ensureGitignore, npm install @diveeoi/plugin background forkDetach, mergeDeep command/agent/mode/plugins, Flag.OPENCODE_CONFIG_DIR, OPENCODE_CONFIG_CONTENT, activeAccount via accountSvc, console token, managedDir, managed preferences, mode->agent conversion, OPENCODE_PERMISSION JSON, tools->permission mapping, username fallback via os.userInfo, autoshare->share, disable flags for compaction; state via InstanceState.make; get/directories/getConsoleState/waitForDependencies/update/invalidate/updateGlobal (json vs jsonc patch handling, changed flag, invalidate on change); permission from state; defaultLayer provides EffectFlock, FSUtil, Env, Auth, Account, Npm, FetchHttpClient; node with httpClient etc

## session flow notes
- LLM preparation is provider-aware with per-provider quirks (Azure, Copilot, OAuth, opencode)
- Config is hierarchical: global (Global.Path.config) -> flagged -> project -> .opencode dirs -> env content -> account remote -> managed prefs -> flag overrides

## server.ts notes
- Already covered in draft 01, but cross-check: config directories discovery via ConfigPaths.directories, InstanceState per directory

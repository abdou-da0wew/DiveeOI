# Draft 08 — App Server State / Effect Adapter / Profiler (files 33-37)

## app/context/server.tsx (322 lines)
- StoredProject {worktree,expanded}, StoredServer string|HttpBase|Http, ServerProjectState {projects Record<string,StoredProject[]>, lastProject Record<string,string>}, HEALTH_POLL 10s
- normalizeServerUrl trim add http:// strip trailing /, serverName returns displayName else stripped url, isLocalHost check, isRecord, migrateCanonicalLocalServerState handles canonicalLocalServer != local deduplication via worktrees Set
- createServerProjects(scope Store): current()=>projects[scope()], open/close/expand/collapse/move/last/touch via setStore
- resolveServerList deduplicates Map Key->Any merging props first then stored strings/objects
- ServerConnection namespace: HttpBase {url username password}, Http {type http http HttpBase authToken?}, Sidecar {type sidecar http HttpBase variant base|wsl+distro}, Ssh {type ssh host http HttpBase}, Any Http|Sidecar|Ssh, key(conn) => http url or wsl:distro or sidecar or ssh:host, Key brand string, builtin local helpers
- nextServerAfterRemoval filters remaining fallback
- ServerProvider via createSimpleContext gate init props defaultServer canonicalLocalServer servers: persisted global server.v3 migrate callback, store {list projects lastProject}, allServers memo resolveServerList, state active defaultServer, setActive/add(normalize url batch find existing update or push set active) remove -> next server batch filter, isReady memo ready()&&active, scope -> ServerScope.fromServerKey, projects via createServerProjects with per-key caching map projectStores, current memo find active else first, isLocal memo, returns 10 fields

## effect-drizzle-sqlite/session.ts (218 lines)
- EffectSQLiteSession<TRelations> extends Drizzle SQLiteEffectSession with EffectSQLiteQueryEffectHKT error EffectDrizzleQueryError context never; constructor client SqlClient dialect relations options logger/cache
- prepareQuery => new SQLiteEffectPreparedQuery delegating execute to this.execute with logger/cache mapping, prepareRelationalQuery similarly
- execute(query params method): client.unsafe(query.sql, params) then values vs get vs withoutTransform
- isInTransaction checks Effect.serviceOption transactionService Some?
- executeTransactionStatement connection.executeUnprepared void
- withTransaction<A,E,R>: uninterruptibleMask withFiber check existing transactionService option -> reuse scope+connection else make Scope reserve, id increment, connection pipe flatMap finalizers etc; handles begin deferred vs savepoint effect_sql_N, provides context transactionService [connection,id], handles commit/rollback with savepoint release, deferred constraint handling, nested transaction savepoints rollback to
- transaction() delegates to withTransaction creating EffectSQLiteTransaction, subclass overrides transaction -> session.transaction

## http-recorder/src/index.ts (18 lines)
- Simple barrel re-export HttpRecorder http socket namespaces from effect.js socket.js, types CassetteMetadata RecorderOptions RedactOptions RequestMatcher RequestSnapshot

## profiler/src/core.ts (366 lines)
- config enabled DIVEEOI_PROFILER=1 sampleRate, registry Map NodeStats activeCounters callCounts states Map GaugeStats scopeListeners WRAPPED symbol AnyFn ScopeNode {key parent} als AsyncLocalStorage ScopeBegin {key t0 sample mem0 caller node}
- isEnabled, _configure, wrap(key,fn): if disabled or already wrapped return, else wrapped function enters _beginScope try fn apply if promise then wrap then/catch _endScope else _endScope immediate catch -> add WRAPPED symbol
- instrument mods object wrapping function props
- measure/measureAsync scope wrappers, scope(key) manual begin/end handle
- gauge/gaugeDelta track states min/max/sum/count, readRawMem via Bun.memoryUsage fallback process.memoryUsage, processMem returns MB, _beginScope: parent from als, node, callCounts increment sample logic via floor(count*rate) comparison, activeCounters++, stats concurrentMax, als.enterWith
- _endScope: decrement activeCounters restore parent, durMs via hrtime bigint, event with error caller heapDelta rssDelta if sampled, accumulate registry, reportScope to listeners
- snapshot aggregates functions/files/modules/classes via mergeInto segment helpers, total sum, active map, stateSnapshot clone
- _reset clear maps, _onScope register listener, accumulate creates stats with calls/errors totalMs min/max concurrent heapDelta rssDelta, mergeInto sums, reportScope loops listeners, parseRate validates 0-1, segment splits ., classSegment 4+ parts, lastSegment, round2

## Takeaways
- Server state is sophisticated with persistent storage, multi-server dedup, WSL-aware sidecars, migration for canonical local server
- Effect adapter handles nested savepoint transactions with proper interruption semantics
- Profiler uses AsyncLocalStorage call tree with sampling, concurrent tracking, memory deltas

# Draft 07 — Session Prompt & Loop (files 29-32, prompt.ts 1778 lines)

## prompt.ts deep read (1778 lines)
- Imports LayerNode PermissionV1 SessionV1 os SessionID MessageID PartID MessageV2 SessionRevert Session Agent Provider AI tool jsonSchema SessionCompaction SystemPrompt Instruction SessionMemoryIntegration Plugin MAX_STEPS txt ToolRegistry MCP LSP ulid ChildProcess spawners Command Config ConfigMarkdown SessionSummary NamedError SessionProcessor Tool Permission SessionStatus LLM Shell FSUtil Truncate Image decodeDataUrl Process Cause Effect Latch Stream etc InstanceRef InstanceState TaskTool MessageV2 ops SessionRunState RuntimeFlags EventV2Bridge Database SessionEvent
- STRUCTURED_OUTPUT_DESCRIPTION + SYSTEM_PROMPT constants, isOrphanedInterruptedTool (error+interrupted), estimateMessageTokens (chars/4), applyTokenWindow (find last compaction summary idx, estimate tokens backward until maxTokens cut, respect compaction boundary)
- Interface cancel/prompt/loop/shell/command/resolvePromptParts; Service @opencode/SessionPrompt; layer Effect.gen with 20 services (status sessions agents provider processor compaction plugin commands config permission fsys mcp lsp registry truncate image spawner scope instruction state revert summary sys llm events flags database)
- ops() returns TaskPromptOps with cancel resolvePromptParts prompt
- cancel -> state.cancel
- resolvePromptParts(template): InstanceState.context, parts=[text template], ConfigMarkdown.files(template) -> for each name dedup seen -> if ~/ -> home else resolve worktree, fsys.stat Option none -> agents.get(name) if found push agent part else dir else push file Part url filename mime Directory vs text/plain with concurrency unbounded
- titleGenerated Set, generateTitle({sessionID,messageContent}): only root default title not generated before, get small model or session model via provider.getSmallModel / getModel, llm.stream with title agent small true Stream filter textDelta map mkString CatchCause log error -> cleaned text strip <think> split lines trim find non-empty >100 truncate 97+... setTitle
- handleSubtask({task,model,lastUser,sessionID,session,msgs}): InstanceState.context promptOps registry.named taskModel via getModel if task.model else model, create Assistant message via updateMessage, create ToolPart via updatePart task tool id running, trigger tool.execute.before, get taskAgent via agents.get if missing NamedError Unknown, execute via taskTool.execute with abort controller, metadata callback updatePart, ask via permission.ask with merged rules, catchCause -> error log + onInterrupt finalize aborted tool, then attachments mapping, trigger after, finalize assistant finish tool-calls completed etc, if no result error part; if task.command add synthetic user summarize message
- shellImpl(input, ready Latch): uninterruptibleMask restore -> markReady, createUserMessage+userPart synthetic, Assistant msg via updateMessage, ToolPart ShellID.ToolID running, optional experimental Event Shell.Started publish, cwd directory, shellEnv plugin shell.env, ChildProcess.make Shell.preferred args cwd TERM dumb stdin ignore forceKill 3s, stream decodeText handle.all collect output chunks update status, handle.exitCode scoped, exit via failCause or interrupt aborted flag, finish publishes Shell.Ended, updates message completed, tool part completed
- getModel(providerID,modelID,sessionID): provider.getModel exit -> if success return else ModelNotFoundError publish Session.Error with suggestions -> die
- currentModel(sessionID): SELECT model FROM session table if present parse else findMessage newest user model else provider.defaultModel
- createUserMessage(input: PromptInput): resolve agent via agents.get or defaultInfo else NamedError, SELECT agent/model from DB, model = input.model ?? agent.model ?? currentModel, same check, variant resolution via provider.getModel if same+ag.variant, create SessionV1.User info with id ascending role user time created tools agent model providerID/modelID variant system format, publish AgentSwitched/ModelSwitched if changed via SessionEvent, addFinalizer instruction.clear, Draft assign helper PartID.make/ascending, resolvePart function per PromptInput part type file: handle MCP resource source -> readResource publish, data: text/plain -> decodeDataUrl etc, file: -> URLs data: vs file: -> switch: data text/plain -> 3 pieces, file: -> fsys.isDir -> exec Read tool via registry.named bypassCwdCheck, LSP documentSymbol for single line offset handling, directory -> execRead, else binary base64 data url; agent -> Permission.evaluate hint + synthetic text; text -> simple; resolvedParts via forEach flat assign, plugin trigger chat.message, parts normalize images via image.normalize ResizerUnavailable fallback, decode validation log, sessions.updateMessage/Part, reduce to Prompt files/agents/synthetic, dual write Prompted/Synthetic events if experimentalEventSystem, return info parts scoped
- prompt(input): revert cleanup, createUserMessage, sessions.touch, background title generation if real user message, permissions array from input.tools deny/allow setPermission, if noReply return else loop
- lastAssistant helper find newest non-user, runLoop(sessionID): while true -> status busy, msgs = MessageV2.filterCompactedEffect, latest = latest() -> user/assistant/finished/tasks, hasToolCalls check lastAssistantMsg parts tool not providerExecuted not orphaned, if finish not tool-calls and no hasToolCalls and lastUser < lastAssistant break with memory extract forkDetach, step++ getModel, token window overflow.usable, pop task -> subtask handle continue, compaction task process overflow check, agent lookup, maxSteps check, create Assistant msg, finalizeInterruptedAssistant, processor.create via handle, outcome gen: bypassAgentCheck from lastUser parts agent, promptOps, SessionTools.resolve with plugin/permission/ToolRegistry/MCP/Truncate, structured output tool if format json_schema, summary fork step1, plugin transform, sys.system+instruction+toModelMessages, system pushes STRUCTURED_OUTPUT_SYSTEM_PROMPT and TOON hint, handle.process with user agent permission sessionID parentSessionID system modelMsgs isLastStep MAX_STEPS toolChoice required etc, structured capture break, finished check content-filter -> error, json_schema missing -> error, result stop break compact create else continue; compaction.prune fork, return lastAssistant
- ensureInstanceRef helper wraps effect with InstanceRef fallback to InstanceState.context
- loop(input): state.ensureRunning lastAssistant ensureInstanceRef runLoop
- shell(input): Latch.make then state.startShell shellImpl
- command(input): Tracer info, commands.get else NotFound hint published Session.Error throw, agentName via cmd.agent ?? input.agent, parse input.arguments via argsRegex/quoteTrim, placeholderRegex $N handling last collects remainder, $ARGUMENTS handling, append if no placeholders and args, ConfigMarkdown.shell matches run Process.text with shell preferred, taskModel from cmd.model/agent/input.model/currentModel, getModel validates, agent lookup, resolvePromptParts(template), dedup inputFiles, uniqueTemplateParts, isSubtask flag, parts either subtask[] or templateParts+input.parts, userAgent/Model selection, plugin command.execute.before, prompt call, publish Command.Executed
- Exports PromptInput Schema Struct sessionID messageID optional model agent noReply tools Format system variant parts Union Text/File/Agent/Subtask, LoopInput Class, ShellInput Struct, CommandInput Struct, createStructuredOutputTool helper returning AI tool with jsonSchema validation
- Bash/etc regexes, node LayerNode.make layer deps 20+ nodes including SessionMemoryIntegration etc, self-reexport

## Observations
- Prompt loop is core orchestration: message creation -> token windowing -> task handling -> compaction -> LLM via processor -> tool loop -> finish conditions (structured output, content-filter, stop, compact)
- File part resolution is elaborate: supports MCP resources, data URLs, local files via Read tool with LSP range support and binary handling
- Title generation is opportunistic small-model streaming in background fiber
- Shell implementation is fully isolated with abort handling and metadata streaming

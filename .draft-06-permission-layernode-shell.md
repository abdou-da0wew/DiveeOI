# Draft 06 — Permission / LayerNode / CORS / Shell (files 25-28)

## permission/index.ts (234 lines)
- Events Asked/Replied via EventV2, Interface ask(reply asks permission+patterns/ruleset), reply(reply=reject/once/always), list
- PendingEntry {info deferred}, State {pending Map, approved Rule[]}
- evaluate(permission, pattern, ...rulesets): findLast match via Wildcard.match on both permission and pattern else default ask * 
- Service @opencode/Permission
- layer Effect.gen EventV2Bridge + InstanceState.make State pending+approved with finalizer fails all deferred with RejectedError on dispose
- ask: get approved+pending, split ruleset vs request, loop patterns evaluate -> if deny fail DeniedError else if allow continue else needsAsk true; if !needsAsk return; create id ascending, info Request, deferred make void/Rejected|Corrected, pending set, publish Asked, await deferred ensuring delete
- reply: needs Config.Service, get state, existing pending lookup else NotFoundError, delete publish Replied, if reject -> fail deferred Corrected/Rejected + fail all other same session pending + publish reject each; if once -> succeed and return; if always -> push approved rules from info.always then compute fromConfig(config.permission) then for each other same session pending check every pattern evaluate==allow -> publish always + succeed
- list returns pending values
- expand supports ~/ $HOME, fromConfig converts permission object to Ruleset (string value -> pattern * else entries), merge flat, disabled(tools,ruleset) returns Set of denied tools mapping edit/write/patch->edit
- defaultLayer provide EventV2Bridge, node LayerNode.make

## db/effect/layer-node.ts (102 lines)
- RuntimeLayer Layer<never,unknown,unknown>, AnyNode, NodeList, Output/Error/Missing/CheckDependencies conditional types
- $OutputType/$ErrorType symbols, Node<A,E> {kind layer|group, implementation? Layer.Any, dependencies AnyNode[], symbols}
- make<Implementation,Items>(implementation, dependencies & CheckDependencies): returns Node with kind layer
- group<Items>(dependencies): Node with kind group empty implementation
- Replacement {source,replacement}, CheckReplacementErrors, replaceWithNode(source,replacement) and replace(source, Layer) makes Node
- buildLayer<A,E>(node, {replacements?}): map replacements, cache Map Node->RuntimeLayer, visiting Set + stack for cycle detection ids Map; visit = (input) => node=replacements.get(input)??input, cached check, cycle detection throws Cycle detected graph, ids assign, dependencies map visit, result: group -> mergeAll or empty, layer -> implementation or Layer.provide(implementation, dependencies), cache and return; final cast Layer<A,E,never>

## api/cors.ts (54 lines)
- opencodeOrigin /^https:\/\/([a-z0-9-]+\.)*opencode\.ai$/
- CorsOptions {cors? string[]}, CorsConfig = Context.Reference undefined
- isAllowedCorsOrigin(input,opts): true if !input or localhost/127.0.0.1 prefix or oc://renderer or tauri or opencodeOrigin test or isPrivateOrigin else opts.cors includes
- isAllowedRequestOrigin adds sameHost check via new URL host equality
- isPrivateOrigin: split hostname 4 parts parse a,b if NaN false else 10/172.16-31/192.168/100.64-127 return true

## server/tool/shell.ts (690 lines)
- Constants MAX_METADATA 30k, CWD set, FILES set multi-platform, CMD_FILES, FLAGS, SWITCHES, types Part Scan Chunk, resolveWasm fileURLToPath logic, parts(node) extract Part[] from tree-sitter Node skipping sep/redirection, source(node) parent redirected, commands descendantsOfType command, unquote, home ~/ expansion via os.homedir, envValue case-insensitive win, auto HOME/PWD/PSHOME, expand unquote + replace ${env:}/$env:/$HOME etc + home, provider Filesystem:: prefix handler, dynamic detection (subshell $() ${} etc), prefix finds *?.[, pathArgs filters flags, preview truncates 30k, tail keeps last lines/bytes with UTF handling
- parse Effect.promise parser().then parse, ask Effect handles dirs-> external_directory permission and patterns->ShellID.ToolID
- cmd(shell, command,cwd,env): if win && ps -> ChildProcess.make shell -NoLogo etc else ChildProcess.make command with shell param
- parser lazy loads web-tree-sitter wasm for bash and powershell with Language.load cached
- ShellTool = Tool.define ToolID Effect.gen deps Config ChildProcessSpawner FSUtil Truncate Plugin RuntimeFlags; defaultTimeout 2min; cygpath via cygpath -w, resolvePath windows posix via cygpath + normalizePath, argPath expand->prefix->dynamic check->resolvePath, collect: Scan dirs/patterns/always, iterate commands, handle FILES vs CMD_FILES, pathArgs->argPath-> fs.isDir -> dir logic, patterns always via source and BashArity.prefix
- shellEnv plugin trigger shell.env
- run: limits trunc.limits, burstCfg config.tool.burst, keep maxBytes*2, StreamLimiter with burst config, state full/last/list/used/file/sink/cut/expired/aborted, closeSink ensures stream end, ctx.metadata initial, Effect.scoped spawner.spawn(cmd) -> handle.all decodedText via Stream.runForEach with limiter check abort vs truncate, manage keep truncation, full overflow to trunc.write -> file sink, metadata updates, abort/timeout handling via callback abort + sleep (timeout+100) raceAll exit/abort/timeout, kill on abort/timeout, then meta builder, raw join, tail, trunc.write if cut, output composition with truncated notice + shell_metadata, returns {title,metadata,output}
- Outer Tool.define returns description from ShellPrompt.render based on Shell.name/limits/timeout, parameters, execute resolves cwd via resolvePath, validates timeout, detects ps, scoped parse+collect+ask, run

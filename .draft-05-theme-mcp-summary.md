# Draft 05 — Theme / MCP / Summary (files 21-24)

## ui/theme/context.tsx (370 lines)
- ColorScheme light/dark/system, STORAGE_KEYS THEME_ID/COLOR_SCHEME/THEME_CSS_LIGHT/DARK, THEME_STYLE_ID oc-theme
- files glob import.meta.glob themes/*.json lazy, ids sorted, known Set
- names map 40+ themes display names oc-2 OC-2 etc
- normalize oc-1->oc-2, read/write/drop localStorage wrappers, clear drops css keys
- ensureThemeStyleElement creates style#oc-theme in head, getSystemMode matchMedia prefers-color-scheme
- applyThemeCss(theme,themeId,mode): resolve tokens via resolveThemeVariant + themeToCss and v2 resolver, write cached css if not oc-2, build :root with color-scheme + mix-blend-mode + css + v2, remove preload, set dataset.theme/colorScheme, update meta theme-color dark #131010 light #F8F7F7
- cacheThemeVariants loops light/dark writes
- ThemeProvider via createSimpleContext init: read themeId normalized or default oc-2, colorScheme system, mode derived, store {themes {oc-2: oc2ThemeJson}, themeId, colorScheme, mode, previewThemeId, previewScheme}, loads Map promise cache, load(id) normalized lookup store hit else pending else import file then setStore themes, applyTheme wraps applyThemeCss + onThemeApplied, ids() merges known + extra sorted, loadThemes Promise.all, onStorage handler for cross-tab sync, onMount registers storage+media query listeners, migrates legacy oc-1, loads savedTheme cache, createEffect watches themes[themeId] -> applyTheme, setTheme validates known/setStore/write+cache, setColorScheme, returns {themeId,colorScheme,mode,ids,name,loadThemes,themes,setTheme,setColorScheme,registerTheme,previewTheme,previewColorScheme,commitPreview,cancelPreview}

## ui/components/button.tsx (33 lines)
- Wraps Kobalte Button primitive, props size small/normal/large variant primary/secondary/ghost icon IconProps name, splitProps, renders Kobalte with data-component button data-size variant data-icon, classList merger, Show icon -> Icon size small

## server/mcp/index.ts (939 lines)
- DEFAULT_TIMEOUT 30s, CLIENT_OPTIONS capabilities only roots, Resource schema name uri description mimeType client, ToolsChanged event mcp.tools.changed, BrowserOpenFailed, Failed NamedError MCPFailed, NotFoundError, MCPClient=Client, createClient(directory) new Client opencode version CLIENT_OPTIONS setRequestHandler ListRoots returns directory file URL
- Status union connected/disabled/failed/needs_auth/needs_client_registration, pendingOAuthTransports Map, PromptInfo ResourceInfo McpEntry, isMcpConfigured, remoteURL, CreateResult/AuthResult, State {config status clients defs}, Interface 14 methods status clients tools prompts resources add connect disconnect getPrompt readResource startAuth authenticate finishAuth removeAuth supportsOAuth hasStoredTokens getAuthStatus
- Service @opencode/MCP
- layer Effect.gen: deps McpAuth, EventV2Bridge; connectTransport Effect.acquireUseRelease with withTimeout client.connect; DISABLED_RESULT; connectRemote(key, mcp remote): check oauthDisabled/oauthConfig, remoteURL validation, create McpOAuthProvider if not disabled with clientId/secret/scope/callbackPort/redirectUri and auth, transports [StreamableHTTP, SSE] both with authProvider+headers, loop tries Streamable then SSE, catch maps UnauthorizedError-> needs_client_registration or needs_auth with toast events, else failed; connectLocal(key, mcp local): split command, baseDir InstanceState.directory cwd resolve, StdioClientTransport stderr pipe command args cwd env including BUN_BE_BUN for opencode, connectTransport; create(key,mcp): if disabled return disabled, else connectRemote/Local -> if no client log warning return status else fetch defs via McpCatalog.defs if tools capability else error, catchCause -> close client on failure -> failed status handling interrupts; cfgSvc Config.Service; killTree SIGTERM via process.kill; watch(s,name,client,bridge,timeout): onclose delete clients defs set failed log publish ToolsChanged, setNotificationHandler LoggingMessage -> bridge serverLog, ToolListChanged -> refetch defs -> publish ToolsChanged; serverLog maps level to log functions; initialCfg Config.get(); InstanceState.make State loop config entries isMcpConfigured check enabled false -> disabled else create -> store status/client/defs watch; addFinalizer closes all clients kill PIDs; closeClient helper; storeClient sets status connected, watches; status method merges config keys; clients returns s.clients; createAndStore calls create then storeClient; add stores config then createAndStore; connect/disconnect handle enabled toggle; requestTimeout helper resolves s.config vs static; tools collects from connected clients via McpCatalog.convertTool sanitized key; collectFromConnected generic; prompts/resources via McpCatalog; withClient helper guard client exists then tryPromise; getPrompt/readResource via withClient; getMcpConfig/requireMcpConfig lookups; startAuth validates remote + oauth not false, effectiveRedirectUri logic, ensureRunning callback server, random oauthState 32 bytes hex, updateOAuthState, capture onRedirect, create transport try client.connect capturing UnauthorizedError -> pendingTransport; authenticate: startAuth if no authorizationUrl -> direct connect else open browser open(result.url) callback promise waitForCallback check storedState CSRF -> finishAuth; finishAuth transport.finishAuth, clearCodeVerifier, createAndStore; removeAuth/supportsOAuth/hasStoredTokens/getAuthStatus wrappers; defaultLayer provides McpAuth EventV2Bridge Config CrossSpawnSpawner FSUtil; node 4 deps

## server/session/summary.ts (165 lines)
- unquoteGitPath handles quoted git paths with octal/n escapes via Buffer
- Interface summarize(sessionID,messageID), diff(sessionID,messageID?), computeDiff(messages)
- Service @opencode/SessionSummary
- layer gen sessions Snapshot events config; computeDiff scans first step-start snapshot as from last step-finish as to -> snapshot.diffFull else []; summarize sets summary 0s publish Diff empty, if snapshot false return, fetch all messages, filter by messageID or assistant parentID, if not user return, computeDiff set target.info.summary diffs updateMessage; diff finds message by id role user return diffs mapped unquote file; defaultLayer suspends with Session Snapshot EventV2Bridge Config; node deps

## Cross-cutting observations so far
- MCP system is full-featured with dual transport fallback, OAuth complete with callback server, state file handling, process tree killing
- Theme system has runtime loading, cross-tab sync, preview transaction (previewTheme/previewColorScheme + commit/cancel)
- Session summary is minimal diff tracking via Snapshot service

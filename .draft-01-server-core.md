# Draft 01 — Server Core (files 1-4)

Files read: main.ts, server/server.ts, routes/instance/httpapi/server.ts, session/session.ts, session/processor.ts (1085 lines), tool/registry.ts (521 lines)

## main.ts (73 lines)
- Imports profiler, Effect, AdaptiveResourceService, handleCLI, Server, DevMonitor, checkCtx7Update
- CLI handled first: `handleCLI(argv.slice(2))` — if handled, exit 0
- PORT default 4097, HOST 0.0.0.0, CORS split from env
- EMERGENCY_RSS_MULTIPLIER = 1.5
- `Server.listen({ port, hostname: host, cors })` — awaits, logs URL
- DevMonitor.start(), background ctx7 update via Effect.runFork with catchCause
- Adaptive GC: ManagedRuntime.make(AdaptiveResourceDefaultLayer), runFork forever loop:
  - getCurrentTargets() -> { gcIntervalMs, rssTargetMB }
  - sleep(gcIntervalMs)
  - check process.memoryUsage().rss vs rssTargetMB*1.5, Bun.gc(true) emergency
  - if profile === "critical" log degraded mode
  - Bun.gc(true) regular
- Shutdown on SIGINT/SIGTERM -> server.stop(true) then exit 0
- try/catch -> process.exit(1)

## server.ts (254 lines)
- Imports init-projectors (side-effect), NodeHttpServer, Effect layers, HttpRouter, HttpServer, OpenApi, createServer, profiler, MDNS, HttpApiApp, lifecycle, WebSocketTracker, PublicApi, CorsOptions
- Global AI_SDK_LOG_WARNINGS = false
- Types: Listener, ServerApp, ListenOptions (CorsOptions + port/hostname/mdns), ListenerState (scope, server, http, websockets), EffectListener
- ListenerServerService = Context.Service("@diveeoi/ListenerServer") with closeAll effect
- Default = lazy(() => handler = HttpApiApp.webHandler().handler, app.fetch/request wrappers with profiler measureAsync)
- openapi() => OpenApi.fromApi(PublicApi)
- url global mutable
- listen(opts) => Effect.runPromise(listenEffect(opts)) then wrap stop to runPromiseExit
- listenEffect = Effect.fn("Server.listen") -> startWithPortFallback, tcpAddress, makeURL, setupMdns, makeStop; returns hostname/port/url/stop
- listenerLayer(opts, port) => HttpRouter.serve(HttpApiApp.createRoutes(opts), { middleware: disposeMiddleware, disableLogger, disableListenLog }).pipe(provideMerge WebSocketTracker, provideMerge serverLayer, provide ConfigProvider fromEnv)
- startWithPortFallback: if port !==0 use listenWithFallback else try 4096 then 0
- listenWithFallback: startListener with retry up to 16 times on EADDRINUSE, scanning upward
- isAddressInUse: checks error._tag === "ServeError" and cause.code === "EADDRINUSE"
- startListener: Scope.makeUnsafe(), Layer.buildWithMemoMap(listenerLayer, memoMap, scope), provide HttpApiApp.context, onError close scope, map to ListenerState
- tcpAddress: if server.address._tag === "TcpAddress" return else close scope and die
- makeURL: new URL("http://localhost") set hostname/port
- setupMdns: if opts.mdns && port && hostname not loopback -> MDNS.publish, cached unpublish, addFinalizer; else warn if mdns but loopback
- makeStop: cached forceClose and closeScope, returns (close?) => unpublishMdns, forceClose if close, closeScope, clear global url if matches
- forceClose: Effect.all([http.closeAll, websockets.closeAll])
- serverLayer: createServer(), serverRef {closeStarted, forceStop}, monkey-patch server.close to track, Layer.mergeAll(NodeHttpServer.layer(...), Layer.succeed(ListenerServerService))

## routes/instance/httpapi/server.ts (312 lines)
- Huge imports: 40+ services (Account, Agent, Auth, BackgroundJob, Command, Config, Workspace, Env, EventV2Bridge, Format, Git, Installation, LSP, MCP, McpAuth, Permission, Plugin, InstanceStore, Project, Vcs, ProviderAuth, Provider, Question, SessionMemoryIntegration, MemoryScheduler, SessionCompaction, Instruction, LLM, SessionProcessor, SessionPrompt, SessionRevert, SessionRunState, Session, SessionStatus, SessionSummary, Todo, SessionShare, ShareNext, Skill, Discovery, Snapshot, Storage, ToolRegistry, Truncate, Worktree, RuntimeFlags, MoveSession, Database, AdaptiveResource, LayerNode, httpClient, EventV2, ModelsDev, Npm, ProjectV2, ProjectCopy, PtyTicket, Ripgrep, SessionProjector, CorsConfig, serveUIEffect, ServerAuth, InstanceHttpApi, Api, JwtAuth, PublicApi, authorization layers, handlers, middleware)
- context = Context.makeUnsafe empty Map
- cors function: HttpRouter.middleware(HttpMiddleware.cors({ allowedOrigins: isAllowedCorsOrigin, maxAge 86400, credentials true }), global true)
- authOnlyRouterLayer = authorizationRouterMiddleware.layer -> provide ServerAuth.Config
- httpApiAuthLayer = authorizationLayer -> provide ServerAuth.Config
- ptyConnectHttpApiAuthLayer, serverHttpApiAuthLayer, workspaceRoutingLive (with Socket WebSocket), rootApiRoutes (RootHttpApi with controlHandlers+controlPlaneHandlers+globalHandlers, schemaErrorLayer, httpApiAuthLayer), eventApiRoutes (EventApi with eventHandlers, httpApiAuthLayer+workspaceRoutingLive+instanceContextLayer), ptyConnectApiRoutes (PtyConnectApi with ptyConnectHandlers, pty auth layers), instanceApiRoutes (InstanceHttpApi with 15 handler groups)
- instanceRoutes = instanceApiRoutes provide [httpApiAuthLayer, workspaceRoutingLive, instanceContextLayer, schemaErrorLayer]
- serverRoutes = Api provide handlers, PluginPtyEnvironment, JwtAuth, serverHttpApiAuthLayer+v2SchemaErrorLayer
- docResponse = lazy HttpServerResponse.jsonUnsafe(OpenApi.fromApi(PublicApi))
- docRoute = HttpRouter.use add GET /doc -> docResponse, provide authOnlyRouterLayer
- uiRoute = HttpRouter.use FSUtil + HttpClient + RuntimeFlags -> router.add("*", "/*", serveUIEffect), provide authOnlyRouterLayer
- themeRoute = rawThemeRoute provide authOnlyRouterLayer
- RouteRequirements union type
- app = LayerNode.group([Npm, FSUtil, Database, Auth, Account, Config, Env, Git, Ripgrep, Storage, Snapshot, Plugin, ModelsDev, Provider, ProviderAuth, Agent, Skill, Discovery, Question, Permission, Todo, Session, SessionProjector, SessionStatus, BackgroundJob, RuntimeFlags, EventV2Bridge, SessionRunState, SessionProcessor, SessionCompaction, SessionRevert, SessionSummary, SessionPrompt, Instruction, LLM, LSP, MCP, McpAuth, Command, Truncate, Format, Project, Vcs, Workspace, Worktree, Installation, ShareNext, SessionShare, InstanceStore, httpClient, EventV2, ProjectV2, ProjectCopy, PtyTicket])
- createRoutes(corsOptions): Layer.mergeAll(rootApiRoutes, eventApiRoutes, ptyConnectApiRoutes, instanceRoutes, serverRoutes, docRoute, themeRoute, memoryExtractRoute, uiRoute).pipe(provide [errorLayer, compressionLayer, corsVaryFix, fenceLayer, cors(corsOptions), MoveSession.defaultLayer, HttpServer.layerServices, AdaptiveResourceDefaultLayer], provide LayerNode.buildLayer(app), provideMerge SessionMemoryIntegration.node, provideMerge ToolRegistry.node, provide CorsConfig, provide Observability.layer)
- routes = createRoutes()
- webHandler = lazy HttpRouter.toWebHandler(routes, { disableLogger true, memoMap, middleware disposeMiddleware(traceMiddleware) })

## session.ts (1164 lines)
- Imports LayerNode, PermissionV1, Slug, SessionV1, serviceUse, path, BackgroundJob, Decimal, Provider Metadata Usage, InstallationVersion, Database, Global, makeRuntime, EventV2Bridge, EventV2, SessionV2, SessionExecution, NotFoundError, Provider, drizzle-orm ops, PartTable SessionTable ProjectTable, MessageV2, InstanceContext, InstanceState, Snapshot, ProjectV2, WorkspaceV2, SessionID MessageID PartID, SessionMemoryIntegration Memory MemoryConfig, Effect Layer Context Schema, AbsolutePath, RuntimeFlags, ProviderV2 ModelV2
- runtime = makeRuntime(Database.Service, Database.defaultLayer)
- parentTitlePrefix = "New session - ", childTitlePrefix = "Child session - ", isDefaultTitle regex test
- fromRow: maps SessionRow to Info (summary, share, revert, permission, tokens, time), handles nulls; toRow inverse
- getForkedTitle: regex " (fork #N)" -> increment else " (fork #1)"
- sessionPath: relative worktree->cwd normalized
- Schemas: Summary, Tokens (with cache), Share, ArchivedTimestamp, Time, Revert, Model, Metadata, Info (Session), ProjectInfo, GlobalInfo (Info + project), CreateInput optional struct, ForkInput, GetInput=SessionID etc, CreatedEventSchema etc, Event = {Created, Updated, Deleted via SessionV1.Event, Diff via EventV2.define session.diff, Error via SessionV1.Assistant error}
- plan(): path to .opencode/plans or Global.Path.data/plans
- getUsage(): safe() clamp, computes input/output/reasoning/cache tokens, handles AI SDK v6 cache double counting, costInfo by context tier, totalNanoAiu copilot, Decimal cost calc
- BusyError TaggedError, Interface with 20 methods (list, listGlobal, create, fork, touch, get, setTitle, incrementTitleAttempts, getTitleAttempts, setArchived, setMetadata, setPermission, setRevert, clearRevert, setSummary, setShare, setWorkspace, diff, messages, children, remove, updateMessage, removeMessage, removePart, getPart, updatePart, updatePartDelta, findMessage)
- Service = Context.Service("@opencode/Session"), use = serviceUse(Service), Patch type
- layer: Layer.effect(Service, gen: db, database, background, events, flags; defines createNext (build Info with Slug descending, InstallationVersion, ctx.project, title default prefix + ISO, db insert ProjectTable onConflictDoNothing, publish Created), get (select by id, fail NotFound, apply titleAttemptCounts), list -> listByProject, listGlobal with SQL conditions, children, remove (recursive children, cancelBackgroundJobs, publish Deleted, events.remove), updateMessage/updatePart publish events, getPart select, create, fork (copy messages/parts mapping IDs), patch (merge partial), touch, setTitle, titleAttemptCounts Map, increment/getAttempts, setArchived/Metadata/Permission/Revert/clearRevert/Summary/Share/Workspace, diff stub, messages (paged if limit/before/after else loop 50 pagination), removeMessage/Part publish, updatePartDelta publish PartDelta, findMessage paginated predicate)
- defaultLayer pipe provide BackgroundJob, Database, EventV2Bridge, SessionExecution.noopLayer, SessionV2, RuntimeFlags
- cancelBackgroundJobs: list jobs filter running and matching sessionID, forEach cancel
- listByProject: conditions eq project_id, workspaceID, path, directory, roots, start, search; order desc time_updated limit 100
- listGlobal generator: similar conditions including cursor/lt, archived filter, fetch projects via inArray, yield with project
- node = LayerNode.make(layer, [BackgroundJob.node, RuntimeFlags.node, Database.node, EventV2Bridge.node, Memory.node])

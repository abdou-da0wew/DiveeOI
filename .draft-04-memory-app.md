# Draft 04 — Memory Service / App Shell (files 16-20)

## memory/memory.ts (346 lines)
- MemoryService interface 18 methods: createNode, getNode, updateNode, deleteNode, addLink, removeLink, getBacklinks, recall, getSessionMemory, bindSessionMemory, createSessionRoot, loadSessionContext, extractFromSession, extractFromMessages, consolidate, search, getRecent, getStats, waitForIndexer, isIndexerReady
- Service = @diveeoi/memory/MemoryService
- makeMemoryService gen: deps NodeService, IndexerService, GraphService, ExtractorService, MemoryConfig, Database.Service, Scope.Scope; init: if config.features.nonBlockingInit -> forkIn scope indexer.initialize with log, else blocking await; sessionLocks = PartitionedSemaphore.make<string> permits 1
- createNode: nodeService.create -> indexer.upsertNode -> extractWikilinks -> upsertLink references -> explicit links
- getNode: indexer.getNode fallback nodeService.get
- updateNode: nodeService.update -> upsertNode -> if patch.content reprocess wikilinks: delete old links then re-extract
- deleteNode: both deletes
- addLink/removeLink/getBacklinks delegate indexer
- recall: filter valid seeds via getNode isSome, if 0 return empty else graph.recall
- getSessionMemory: withPermit sessionLocks sessionId -> getSessionRoot: if none createSessionRoot Untitled else getSessionNodes, return SessionMemory.make
- bindSessionMemory -> indexer.bindSession
- createSessionRoot: createNode type session title Session: title content Root memory node tags session root -> bindSession
- loadSessionContext sessionId maxDepth2 maxNodes15 -> getSessionMemory -> recall seed root
- extractFromMessages -> extractor.extractFromMessages
- extractFromSession -> extractor.extractFromSession(sessionId, messages, config.memoryDir) return nodes
- consolidate: if !enabled return, if <2 nodes return, ensure all linked to root via addLink references
- search -> indexer.searchNodes, getRecent -> indexer.getRecentNodes, getStats -> countNodes/Links/Sessions, waitForIndexer -> waitForReady, isIndexerReady -> isReady
- MemoryLive = Layer.effect Service makeMemoryService

## memory/indexer.ts (510 lines) — detailed
- MemoryIndexSchema: nodes CREATE TABLE IF NOT EXISTS memory_nodes (id PK type title content session_id created updated confidence tags path) + nodesAlterColumns add project_id/project_root/file_exists, links CREATE TABLE memory_links (source_id target_id PK link_type created), indexes 8 idx_* (target, session, type, updated, confidence, content, project, orphaned), session_memories (session_id PK root_node_id FK), projects CREATE TABLE memory_projects (id PK root_path unique name memory_dir last_scanned is_active node_count) + 2 indexes
- IndexerService 21 methods: ready Deferred, isReady, waitForReady, initialize, ; Context.Service
- makeIndexer gen Database.Service + MemoryConfig, ready Deferred.make<void,MemoryError>
- initialize: run nodes/links/sessionMemories/projects + indexes + alter columns catch duplicate column + foreign_keys ON + succeed ready
- rowToNode: parse tags JSON, handle project_id/root/file_exists defaults
- rowToLink: source_id target_id link_type created
- mapDbError pipe mapError MemoryError, withTransaction = mapDbError(db.transaction(()=>effect))
- upsertNode INSERT OR REPLACE 13 cols
- deleteNode DELETE nodes + links OR
- getNode SELECT * WHERE id -> Option
- getNodes IN placeholders joined
- upsertLink INSERT OR REPLACE links
- deleteLink DELETE WHERE source target
- getLinks WHERE source_id, getBacklinks WHERE target_id
- getNodesBySession/Type ordered updated DESC
- searchNodes WHERE LOWER title/content LIKE %query% limit
- bindSession INSERT INTO session_memories ON CONFLICT DO UPDATE
- getSessionRoot SELECT root_node_id Option
- getSessionNodes recursive CTE WITH RECURSIVE graph(id,depth) SELECT root depth0 UNION ALL links outgoing depth<2 UNION incoming depth<2 JOIN memory_nodes limit 20
- getRecentNodes optional since filter
- getLinkedNodes similar CTE with depth param
- countNodes/Links/Sessions SELECT COUNT
- rebuildIndex DELETE nodes/links
- hashProjectPath simple 32bit hash -> proj_hex
- rowToProject mapping, registerProject INSERT ON CONFLICT memory_projects, listProjects active ORDER last_scanned, getNodesByProject, verifyFileExists -> fs.promises access catch -> update file_exists flag, getOrphanedNodes WHERE file_exists=0

## app/entry.tsx (182 lines)
- DEFAULT_SERVER_URL_KEY diveeoi.settings.dat:defaultServerUrl
- getLocale checks navigator.languages for zh prefix else en
- getRootNotFoundError, getStorage/setStorage localStorage wrapper try/catch
- readDefaultServerUrl/writeDefaultServerUrl
- notify: check Notification permission request, inView check visibilityState+hasFocus skip, new Notification with favicon.svg onclick handleNotificationClick
- openLink window.open _blank, back/forward history, restart reload
- root = getElementById root throw if dev and not HTMLElement
- getCurrentUrl: DEV -> VITE_DIVEEOI_SERVER_HOST:PORT else location.origin
- getDefaultUrl: lsDefault or getCurrentUrl
- clearAuthToken: delete auth_token param replaceState
- platform: platform web version pkg.version openLink/back/forward/restart/notify/getDefaultServer/setDefaultServer
- Sentry.init if VITE_SENTRY_DSN with env tags filter Breadcrumbs and GlobalHandlers in prod
- if root exists: auth = authFromToken(search auth_token) ?? VITE_DIVEEOI_SERVER_PASSWORD btoa username:password; clearAuthToken; server Http {type http authToken http{url getCurrentUrl() ...auth}}; render PlatformProvider AppBaseProviders AppInterface defaultServer Key.make(getDefaultUrl()) canonicalLocalServer key(server) servers [server] disableHealthCheck

## app/app.tsx (500 lines) — partial
- Imports ThemeProvider, I18n, Dialog, FileComponent, Marked, Font, Splash, Theme, MetaProvider, Router, QueryClient, Effect, solid-js primitives, all context providers, pages
- Home/Session/NewSession/Login/Register lazy
- SessionRoute: settings, params, search draftId/prompt, sdk, server, tabs; createEffect if newLayoutDesigns and no params.id/draftId and tabs.ready and sdk.directory -> newDraft; render SessionProviders Session preload
- SelectedServerLayout wraps ServerKey -> ServerSDKProvider -> ServerSyncProvider -> ServerScopedShell children
- DraftServerLayout: useServer tabs search draftId -> memo conn via tabs.store draftTab find -> ServerSDKProvider server conn -> ServerSyncProvider -> ServerScopedShell
- DraftRoute: tabs.ready Show when draftId keyed ResolvedDraftRoute fallback Navigate /
- ResolvedDraftRoute: draft memo, directory memo, Show keyed SDKProvider DirectoryDataProvider DraftProviders NewSession
- SharedProviders: SettingsProvider -> BodyDesignClass -> CommandProvider -> HighlightsProvider children
- ServerScopedShell: PermissionProvider -> LayoutProvider -> NotificationProvider -> ModelsProvider -> GlobalPrefSync -> Layout children
- SessionProviders: TerminalProvider -> FileProvider -> PromptProvider -> CommentsProvider
- DraftProviders: same without TerminalProvider
- AppBaseProviders: MetaProvider Font ThemeProvider LanguageProvider UiI18nBridge ErrorBoundary Sentry.captureException QueryProvider WslServersProvider DialogProvider MarkedProvider FileComponentProvider
- ConnectionGate: server useServer checkServerHealth, checkMode blocking/background, createResource Effect.gen check server health loop with Effect health check via checkServerHealth(http) if blocking mode 10s timeout else false; checking memo blocking unresolved/pending; Show fallback Splash pulse, then Show when health latest fallback ConnectionError children
- ConnectionError: others filter, timer setInterval retry 1s cleanup, render Splash + unreachable split by token + retrying + other servers list
- ServerKey Show when server.key keyed
- ExternalThemeLoader: useTheme useServer onMount fetch /_opencode/themes registerTheme
- AppInterface: defaultServer canonicalLocalServer servers router disableHealthCheck; ServerShell = QueryProvider SharedProviders; return ServerProvider GlobalProvider ConnectionGate ExternalThemeLoader Dynamic router root TabsProvider ServerShell AuthProvider children; Routes: /login /register inside SelectedServerLayout -> / and /:dir -> DirectoryLayout -> session/:id? SessionRoute; DraftServerLayout -> /new-session DraftRoute

## Observations
- Memory system is fully SQLite-backed with recursive CTE traversal and semaphore-per-session
- App entry is web-native with platform abstraction already prepared for desktop but currently web-only
- Provider composition is layered: SharedProviders (agnostic) vs ServerScopedShell (per-server) vs SessionProviders (per-session)

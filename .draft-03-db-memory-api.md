# Draft 03 — DB / Memory / API (files 9-15)

## db/database/database.ts (76 lines)
- makeDatabase = EffectDrizzleSqlite.makeWithDefaults()
- Interface { db: DatabaseShape }, Service @opencode/v2/storage/Database
- layer = makeAdaptiveLayer((targets) => Layer.effect(Service, gen: makeDatabase, PRAGMAs: journal_mode WAL, synchronous NORMAL, busy_timeout 5000, cache_size -targets.sqliteCacheMB*1000, mmap_size*2, foreign_keys ON, wal_checkpoint PASSIVE, DatabaseMigration.apply))
- withDbQueryTimeout: useAdaptiveTargets((targets) => timeout dbQueryTimeoutMs -> map timeout to Error DB query timeout)
- layerFromPath(filename) = layer pipe provide sqliteLayer({filename})
- path(): Flag.OPENCODE_DB -> :memory: or absolute or join Global.Path.data; else if InstallationChannel in latest/beta/prod or DISABLE_CHANNEL_DB -> opencode.db else opencode-{channel}.db sanitized
- defaultLayer = unwrap gen layerFromPath(path()) provide Global.defaultLayer
- node = LayerNode.make(layerFromPath(path()), [AdaptiveResourceNode])

## db/database/schema.gen.ts (277 lines)
- Default export { up(tx): Effect.gen with 14 CREATE TABLE statements + indexes }
- Tables: workspace(id,type,name,branch,directory,extra,project_id,time_used FK), data_migration(name,time_completed PK), account_state(id active_account_id/org_id FK), account(id email url access_token refresh_token token_expiry time_* PK), control_account (email+url PK), credential(id integration_id label value connector/method active time_*), event_sequence(aggregate_id seq owner_id PK), event(id aggregate_id seq type data FK->event_sequence), permission(id project_id action resource time_* FK), project_directory(project_id+directory PK + type/strategy), project(id worktree vcs name icon_* time_* sandboxes commands), message(id session_id time_* data FK), part(id message_id session_id time_* data FK), session_context_epoch(session_id baseline agent snapshot baseline_seq replacement_seq revision FK), session_input(id session_id prompt delivery admitted_seq promoted_seq time_created FK), session_message(id session_id type seq time_* data FK), session(id project_id workspace_id parent_id slug directory path title version share_url summary_* metadata cost tokens_* revert permission agent model time_* FK), todo(session_id position PK content status priority time_* FK), session_share(session_id PK id secret url time_* FK)
- Indexes: event_aggregate_seq unique, event_aggregate_type_seq, permission_project_action_resource unique, message_session_time_created_id, part_message_id_id, part_session, session_input_session_pending_delivery_seq, session_input_session_admitted_seq unique, session_input_session_promoted_seq unique, session_message_session_seq unique, session_message_session_type_seq, session_message_session_time_created_id, session_message_time_created, session_project, session_workspace, session_parent, todo_session

## db/session/schema.ts (49 lines)
- ID = Schema.String startsWith "ses" brand SessionID with statics create()=ses_+Identifier.descending(), descending(id?), fromExternal
- Info class SessionV2.Info {id,parentID,projectID,agent AgentV2.ID optional, model ModelV2.Ref optional, cost, tokens {input/output/reasoning/cache {read/write}}, time {created/updated/archived V2Schema.DateTimeUtcFromMillis}, title, location Location.Ref, subpath RelativePath optional}

## api/api.ts (54 lines)
- Api = HttpApi.make("server") .add 21 groups: Health, Location, Agent, Session, Message, Model, Provider, Integration, Credential, Permission, FileSystem, Command, Skill, Event, Pty, Question, Reference, ProjectCopy, Prefs, Auth; annotate OpenApi title opencode HttpApi version 0.0.1; middleware Authorization + SchemaErrorMiddleware

## api/groups/session.ts (215 lines)
- SessionsQueryFields = {workspace optional, limit NumberFromString->PositiveInt, order asc/desc, search}
- SessionsDirectoryQuery = ...Fields + directory AbsolutePath, SessionsProjectQuery = ...Fields + project + subpath, SessionsAllQuery = ...Fields
- withCursor helper maps limit->anchor (SessionV2.ListAnchor)
- SessionsCursor = String brand base64url of JSON of SessionsCursorInput (Union of withCursor queries), with statics make/parse via Buffer base64url + Schema.fromJsonString
- SessionsCursorQuery = {cursor, limit}, SessionsQuery = ...Fields + optional directory/project/subpath/cursor
- SessionGroup = HttpApiGroup.make("server.session") with 7 endpoints: list GET /api/session query SessionsQuery success {data Array SessionV2.Info, cursor {previous,next optional}} error InvalidCursor/InvalidRequest; create POST /api/session payload {id optional, agent optional, model optional, location optional} success data; get GET /api/session/:sessionID param SessionV2.ID success data middleware SessionLocationMiddleware; prompt POST /api/session/:sessionID/prompt param payload {id optional, prompt, delivery optional, resume optional} success SessionInput.Admitted error Conflict/Forbidden/NotFound middleware; compact POST .../compact NoContent error NotFound/ServiceUnavailable; wait POST .../wait NoContent; context GET .../context success Array SessionMessage.Message middleware; annotateMerge OpenApi titles

## llm/src/index.ts (33 lines)
- Re-exports LLMClient, Auth, Provider, isContextOverflow*, RouteModelInput etc, schema, Tool, ToolRuntime, LLM namespace, ProviderDefinition
- Simple barrel

## memory/schema.ts (163 lines)
- Branded IDs: MemoryNodeID, SessionID, ProjectID String brand
- MemoryType literals 8 types, LinkType 4 types
- ProjectInfo Class {id,rootPath,name,memoryDir,lastScanned,isActive,nodeCount}
- MemoryNode Class {id,type,title,content,tags[],sessionId,created,updated,confidence,path AbsolutePath, projectId/root/fileExists optional}
- MemoryLink Class {sourceId,targetId,type,created}
- SessionMemory Class {sessionId,rootNodeId,nodeIds[],loadedAt}
- CreateNodeInput Class {type,title,content,tags,sessionId,confidence optional,links optional}
- PatchNode Class {title/content/tags/confidence/updated optional}
- RecallOptions Class {seedNodes[],maxDepth,maxNodes,types optional,minConfidence optional,timeRange optional,queryTags optional,currentSession optional}
- RecallResult Class {nodes[],edges[],scores Record String->Number}
- ExtractedMemory, ConsolidationInput/Result, Errors MemoryError/NodeNotFound/SessionMemoryError, re-export MemoryConfig

## memory/graph.ts (465 lines)
- RecallContext {currentSession?,queryTags[],seedNodes[],now}, ScoredNode {node,score,depth,path[]}, GraphTraversalResult {nodes Map,edges[]}
- GraphService interface 7 methods: recall, traverse, pureScoreNode, getSubgraph, findPaths, getHubs, getOrphans, getClusters; Context.Service
- TYPE_WEIGHTS: decision 1.0 pattern 0.9 error 0.8 entity 0.7 preference 0.6 fact 0.5 constraint 0.5 session 0.4; RECENCY_HALF_LIFE 30 days
- makeGraphService gen indexer + config: scoreNodePure computes 8 factors: recency exp(-LN2*age/halfLife) weight 0.25, linkDensity min(degree/10,1) 0.20, typeWeight 0.20, confidence 0.10, sessionBoost 0.10 binary, tagOverlap min(overlap/3,1) 0.15, depthPenalty 1-exp(-depth*lambda), seedMultiplier 1.2; weighted sum 0.25+0.20+0.20+0.10+0.10+0.15=1.0 *seedMultiplier -depthPenalty clamp [0,1]
- traverse BFS by depth: frontier seeds depth 0, while frontier && visited<maxVisited: fetch nodes parallel via Effect.all map entry if not visited, getNode, getLinks+getBacklinks, return; then for each result score via scoreNodePure, add to visited, add edges via Map JSON key, push neighbors depth+1 if <maxDepth and not visited; nextFrontier
- recall -> traverse with maxDepth/maxNodes derived, filter by types/minConfidence/timeRange, sort by score desc slice maxResults filter edges between top nodes return nodes/edges/scores
- getSubgraph fetches nodes via getNodes, deduplicate edges within nodeIdSet
- findPaths BFS queue with per-path cycle check path.includes, bidirectional links+backlinks, limit 10 paths
- getHubs recent 1000 nodes scoring by degree sort limit
- getOrphans recent 1000 filter degree 0 limit
- getClusters connected components via BFS with visited Set, filtered minClusterSize sorted by size

## Observations
- DB schema is event-sourced + session-centric with new v2 session tables alongside legacy session table
- Memory graph uses level-order BFS guarantee for shortest depth and pure scoring isolated from DB
- API cursor is opaque base64url JSON with type-tagged union switching
